# Parallelized-Mini-Batch-Gradient-Descent-For-Linear-Regression
An efficient implementation of linear regression using mini-batch gradient descent with parallel computation to accelerate training on large datasets.
